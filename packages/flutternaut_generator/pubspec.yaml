name: flutternaut_generator
description: "CLI tool that scans a Flutter project for @FlutternautView screens and the ValueKeys inside them, writing a keys JSON (grouped by view, list rows detected) for the Flutternaut Test Editor."
version: 0.0.4
homepage: https://flutternaut.app
repository: https://github.com/KhaledMoSha/flutternaut
topics:
  - flutter
  - testing
  - code-generation
  - automation
  - cli
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  analyzer: ">=6.0.0 <11.0.0"
  args: ^2.4.0
  path: ^1.8.0
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.24.0

executables:
  flutternaut_generator: flutternaut_generator
